package bean;

import java.util.ArrayList;
import java.util.List;

public class CarGroup {
	
	public List<Car> A;
	public List<Car> B;
	public List<Car> C;
	public List<Car> D;
	public List<Car> F;
	public List<Car> G;
	public List<Car> H;
	public List<Car> J;
	public List<Car> K;
	public List<Car> L;
	public List<Car> M;
	public List<Car> N;
	public List<Car> O;
	public List<Car> P;
	public List<Car> Q;
	public List<Car> R;
	public List<Car> S;
	public List<Car> T;
	public List<Car> W;
	public List<Car> X;
	public List<Car> Y;
	public List<Car> Z;

	public List<Car> getTotalCityList() {
		ArrayList<Car> total = new ArrayList<Car>();

		if (A != null && A.size() != 0) {
			total.addAll(A);
		}
		if (B != null && B.size() != 0) {
			total.addAll(B);
		}
		if (C != null && C.size() != 0) {
			total.addAll(C);
		}
		if (D != null && D.size() != 0) {
			total.addAll(D);
		}
		if (F != null && F.size() != 0) {
			total.addAll(F);
		}
		if (G != null && G.size() != 0) {
			total.addAll(G);
		}
		if (H != null && H.size() != 0) {
			total.addAll(H);
		}
		if (J != null && J.size() != 0) {
			total.addAll(J);
		}
		if (K != null && K.size() != 0) {
			total.addAll(K);
		}
		if (L != null && L.size() != 0) {
			total.addAll(L);
		}
		if (M != null && M.size() != 0) {
			total.addAll(M);
		}
		if (N != null && N.size() != 0) {
			total.addAll(N);
		}
		if (O != null && O.size() != 0) {
			total.addAll(O);
		}
		if (P != null && P.size() != 0) {
			total.addAll(P);
		}
		if (Q != null && Q.size() != 0) {
			total.addAll(Q);
		}
		if (R != null && R.size() != 0) {
			total.addAll(R);
		}
		if (S != null && S.size() != 0) {
			total.addAll(S);
		}
		if (T != null && T.size() != 0) {
			total.addAll(T);
		}
		if (W != null && W.size() != 0) {
			total.addAll(W);
		}
		if (X != null && X.size() != 0) {
			total.addAll(X);
		}
		if (Y != null && Y.size() != 0) {
			total.addAll(Y);
		}
		if (Z != null && Z.size() != 0) {
			total.addAll(Z);
		}

		return total;
	}
}
